public class Dragon implements Comparable<Dragon> {  
    private int id; //Значение поля должно быть больше 0, Значение этого поля должно быть уникальным, Значение этого поля должно генерироваться автоматически  
    private String name; //Поле не может быть null, Строка не может быть пустой  
    private long age; //Значение поля должно быть больше 0  
    private int weight; //Значение поля должно быть больше 0  
    private Dragon.DragonType type; //Поле не может быть null

    
    public int compareTo (Dragon anotherDragon) {
	//реализация сравнения по полю id
    }
}

public enum Dragon.DragonType {
    WATER,
    UNDERGROUND,
    AIR,
    FIRE;
}

public class ListDragon implements CollectionManager {
    private ArrayList<Dragon> listDragon;

}

interface Reader {
    String read();
}

public class ReaderJson implements Reader {
    @ovveride
    public String read() {
	// реализация считывания текста с файла с помощью класса Scanner;
    }

}

interface Writer {
    void write();
}

public class WriterJson implements Writer {
    @ovveride
    public void write() {
	//реализации записи текста в файл с помощью класса FileOutputReader
    }
}

interface CollectionManager {
	// add
	// remove_by_id id
	// clear
	// remove_at index
}

interface UpdateCollection {
    // update id
    // add_if_min
    // remove_greater
}

interface PrintInfoCollection {
    // help
    // info
   	// show
}

interface FileManager.FileManager {
    // save file_name
    // execute_script file_name
}

interface Exit {
    // exit
}

interface InitCollection {
 public void init() {
    // реализация автоматического заполнения значениями из файла
    }
}


План действий:
1. Добавить дополнительные классы для реализации интерфейсов коллекции
, чтобы разгрузить класс ListDragon
2. Изучить JSON
3. Изучить Javadoc



